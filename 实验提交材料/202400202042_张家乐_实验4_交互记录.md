# 实验4交互记录（整理版）

| 轮次 | 问题/操作 | 结果与结论 |
|---|---|---|
| 1 | 项目测试只有 pytest 是否够？ | 不够。补充单元、集成、端到端三层，并固定 10 条 Golden Set。 |
| 2 | 如何证明每个 Agent 都真正被调用？ | 断言 Supervisor dispatch_log 中的 `to` 顺序为 Intent、POI、Planner、Reviewer。 |
| 3 | LLM-as-Judge 是否已实现？ | 实现固定 Prompt、temperature=0、结构化 Schema，并写入 JSON 报告。 |
| 4 | Judge 的首轮结果如何？ | 10 条全部评分完成，80% 通过；2 条为 constraint_miss。 |
| 5 | 如何解释失败而不回避？ | 说明离线 Fixture 对慢节奏/餐饮偏好表达不足，提出三项可执行优化。 |
| 6（模拟） | 如果 Judge API 失败怎么办？ | 单条记录保存 judge_error，不中断整轮报告；可回退到离线规则测试和人工抽查。 |

本次实验的结论：测试结果既是答辩证据，也是下一轮设计的输入；失败分类应进入回归测试集，而不是只记录一次。
