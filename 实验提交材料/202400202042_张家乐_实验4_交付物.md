# 实验4交付物：测试集、测试代码与协作观察报告

## 1. 测试资产

| 类型 | 文件/产物 | 说明 |
|---|---|---|
| Agent 单元测试 | `tests/test_agent_unit_contracts.py` | Intent、POI、Planner 三类 Agent |
| 多 Agent 集成测试 | `tests/test_multi_agent_core.py` | 调度、返工、重试、工具权限、记忆隔离 |
| 端到端测试 | `tests/test_e2e_evaluation.py` | 10 条 Golden Set 请求 |
| 评估脚本 | `scripts/generate_eval_report.py` | 生成离线可复现 JSON 报告 |
| LLM 评审 | `scripts/run_llm_judge.py` | 结构化 LLM-as-Judge 评分 |
| 评估报告 | `evaluation/latest_report.json`、`llm_judge_report.json` | 成功率、延迟、失败分类 |

## 2. 三组代表性测试用例

| 用例 | 输入 | 期望协作 | 观察结果 |
|---|---|---|---|
| 正常 | 北京文化一日游 | Intent→POI→Planner→Reviewer | 四 Agent 被调度，行程非空，审核通过 |
| 返工 | Planner 首版故意返回空行程 | Reviewer→Planner revise→Reviewer | 发生一次返工后通过，不死循环 |
| 异常 | IntentAgent 模拟 TimeoutError | Supervisor 重试→结构化失败 | 重试两次后返回 failed_agent 和 error_code |

## 3. 实测结果

- `pytest -q`：68 passed（本次项目快照）。
- 离线端到端 Golden Set：10/10 通过，平均延迟约 0.11 ms（Fixture 环境）。
- LLM-as-Judge：10/10 得到结构化评分，Judge 通过率 80%，平均 Judge 延迟约 3.00 秒。
- 失败分类：`constraint_miss` 2 条，主要是“慢节奏/餐饮偏好”只达到基本满足。

## 4. 关键测试代码

完整测试代码已随项目提交至 GitHub 仓库的 `tests/` 目录；以下列出本实验最关键的三类断言，便于直接审阅。

### 4.1 RAG Top-1 检索验收：`tests/test_travel_knowledge.py`

```python
def test_rag_acceptance_set_retrieves_expected_source_in_top_1():
    travel_knowledge.build_index(rebuild=True)
    cases = [
        ("重庆带老人下雨怎么安排", "chongqing_visitor_guide"),
        ("每段交通能都走路吗", "transport_and_pacing"),
        ("资料没有门票价格怎么办", "responsible_trip_checklist"),
        ("远郊地点搜索不到怎么办", "chongqing_visitor_guide"),
        ("一天安排几个景点合适", "transport_and_pacing"),
    ]

    for query, expected_source in cases:
        rows = travel_knowledge.search_travel_knowledge(query)
        assert rows, query
        assert rows[0]["source"] == expected_source, query
```

该测试重建 Chroma 本地索引后，验证 5 条固定查询能在 Top-1 召回预期知识来源。

### 4.2 多 Agent 条件返工：`tests/test_multi_agent_core.py`

```python
def test_supervisor_routes_rejected_draft_back_to_planner_once():
    supervisor = Supervisor({
        "intent_agent": IntentAgent(),
        "poi_research_agent": POIResearchAgent(FixturePoiTool()),
        "planner_agent": FirstDraftNeedsRevision(),
        "reviewer_agent": ReviewerAgent(),
    })
    result = supervisor.run_trip("Plan a relaxed day", "Chongqing")
    dispatches = [entry for entry in result["dispatch_log"]
                  if entry["from"] == "supervisor"]

    assert result["review"]["approved"] is True
    assert [(entry["task_type"], entry["to"], entry["attempt"])
            for entry in dispatches][-2:] == [
        ("itinerary_revise", "planner_agent", 1),
        ("itinerary_review", "reviewer_agent", 1),
    ]
```

`FirstDraftNeedsRevision` 会故意生成空行程，使 Reviewer 驳回。断言验证 Supervisor 只允许一次 `Planner -> Reviewer` 返工，不会无限循环。

### 4.3 端到端 Golden Set：`tests/test_e2e_evaluation.py`

```python
@pytest.mark.parametrize("case_id,user_request,destination", CASES,
                         ids=[item[0] for item in CASES])
def test_end_to_end_trip_cases(case_id, user_request, destination):
    result = make_supervisor().run_trip(user_request, destination, session_id=case_id)

    assert result.get("status") != "failed"
    assert result["intent"]["destination"] == destination
    assert result["itinerary"], case_id
    assert result["review"]["approved"] is True
    dispatches = [item for item in result["dispatch_log"] if item["from"] == "supervisor"]
    assert [item["to"] for item in dispatches][:4] == [
        "intent_agent", "poi_research_agent", "planner_agent", "reviewer_agent",
    ]
```

该测试通过参数化执行 10 条固定旅行请求，验证端到端链路没有失败、目的地识别正确、行程非空、Reviewer 放行，以及四个 Agent 按预期顺序被 Supervisor 调度。

### 4.4 运行命令

```powershell
# RAG 知识检索与 Planner 注入
.\.venv\Scripts\python.exe -m pytest tests\test_travel_knowledge.py -v

# Supervisor 调度、返工、重试、权限与记忆隔离
.\.venv\Scripts\python.exe -m pytest tests\test_multi_agent_core.py -s -v

# 10 条离线端到端 Golden Set
.\.venv\Scripts\python.exe -m pytest tests\test_e2e_evaluation.py -v
```

## 5. 优化前后对比

| 观察问题 | 优化 | 对比结论 |
|---|---|---|
| 规划草案可能不合格 | 加入 Reviewer 返工路由 | 不合格草案不会直接输出，返工次数受限 |
| 工具失败可能中断流程 | Supervisor 重试和结构化失败结果 | 单个 Worker 故障可被上游捕获 |
| 偏好满足难量化 | 引入固定 Prompt、temperature=0 的 LLM Judge | 可记录分项分数和失败类型 |
